// API Configuration
// API calls are proxied through the backend server to keep keys secure

const CONFIG = {
    API_BASE_URL: window.location.origin, // Use same origin as the server
    DEEPSEEK_PROXY_URL: '/api/deepseek'
};

