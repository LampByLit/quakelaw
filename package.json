{
  "name": "q1k3-lawyer-game",
  "version": "1.0.0",
  "description": "Lawyer game with DeepSeek API integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "setup": "npm install && echo. && echo Setup complete! Create a .env file with DEEPSEEK_API_KEY and run 'npm start'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
